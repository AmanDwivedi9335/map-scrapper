export type AccountHealthState='HEALTHY'|'ACTION_REQUIRED'|'LIMITED'|'SUSPENDED';
export interface HealthInput{emailVerified:boolean;organizationActive:boolean;extensionConnected:boolean;planActive:boolean;billingConfigured:boolean;creditsAvailable:number;pendingInvites:number}
export function accountHealth(i:HealthInput){const checks=[{key:'email',label:'Email verified',complete:i.emailVerified,action:'/verify-email'},{key:'organization',label:'Organization created',complete:i.organizationActive,action:'/settings/workspace'},{key:'extension',label:'Extension connected',complete:i.extensionConnected,action:'/settings/extension'},{key:'plan',label:'Plan active',complete:i.planActive,action:'/settings/billing'},{key:'billing',label:'Billing configured',complete:i.billingConfigured||!i.planActive,action:'/settings/billing'}];const score=Math.round(checks.filter(x=>x.complete).length/checks.length*100);const state:AccountHealthState=!i.organizationActive?'SUSPENDED':!i.emailVerified?'ACTION_REQUIRED':i.creditsAvailable<=0?'LIMITED':score===100?'HEALTHY':'ACTION_REQUIRED';return{score,state,checks,warnings:[...(i.creditsAvailable<25?['Low enrichment credits']:[]),...(i.pendingInvites?['Team invite pending']:[])]}}
