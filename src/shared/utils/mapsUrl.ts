export function parseCoordinates(url?:string):{latitude:number;longitude:number}|undefined {if(!url)return; const patterns=[/@(-?\d+(?:\.\d+)?),(-?\d+(?:\.\d+)?)/,/[?!&]query=(-?\d+(?:\.\d+)?)(?:%2C|,)(-?\d+(?:\.\d+)?)/,/!3d(-?\d+(?:\.\d+)?)!4d(-?\d+(?:\.\d+)?)/];for(const p of patterns){const m=url.match(p);if(m){const latitude=Number(m[1]),longitude=Number(m[2]);if(latitude>=-90&&latitude<=90&&longitude>=-180&&longitude<=180)return{latitude,longitude}}}}
export function extractPlaceId(url?:string){if(!url)return; const m=url.match(/[?&]query_place_id=([^&]+)/)||url.match(/!1s([^!]+)/);return m?.[1]?decodeURIComponent(m[1]):undefined}
