import type {BusinessRecord} from '../shared/types/models';export const FIELD_LABELS:Record<string,string>={name:'Business Name',primaryCategory:'Category',address:'Address',phone:'Phone',website:'Website',rating:'Rating',reviewCount:'Review Count',latitude:'Latitude',longitude:'Longitude',googleMapsUrl:'Google Maps URL',searchKeyword:'Search Keyword',searchLocation:'Search Location',source:'Source',scrapedAt:'Scraped At'};
export const valueFor=(r:BusinessRecord,k:string):string|number=>{const v=r[k as keyof BusinessRecord];return Array.isArray(v)?v.join('; '):typeof v==='number'?v:typeof v==='string'?v:''};
export const safeFilename=(phrase:string,format:string,date=new Date())=>`google-maps-${phrase.toLowerCase().normalize('NFKD').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,80)||'leads'}-${date.toISOString().slice(0,10)}.${format}`;
export function download(blob:Blob,filename:string){const url=URL.createObjectURL(blob);chrome.downloads.download({url,filename,saveAs:true},()=>setTimeout(()=>URL.revokeObjectURL(url),1000))}
