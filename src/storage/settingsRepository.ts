import {db} from './db';import {DEFAULT_SETTINGS} from '../shared/constants';import type {Settings} from '../shared/types/models';export const settingsRepository={async get(){return(await db.settings.get('app'))??DEFAULT_SETTINGS},save:(s:Settings)=>db.settings.put(s),async clearAll(){await db.transaction('rw',db.jobs,db.businesses,db.settings,async()=>{await db.jobs.clear();await db.businesses.clear();await db.settings.clear()})}};
